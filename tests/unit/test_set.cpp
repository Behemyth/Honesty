import std;
import synodic.honesty.test;

using namespace synodic::honesty;
using namespace synodic::honesty::literals;

//static_assert(
//	std::ranges::range<Set>,
//	"Set needs to be modelled as a range so that recursive yields can extract the elements with `elements_of`");
